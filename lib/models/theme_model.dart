import 'package:flutter/material.dart';

class ThemeModel {
  String type;
  Color backColor;
  Color bodyColor;
  Color accentColor;
  Color textColor;
  Color subTextColor;
  ThemeModel(this.type, this.backColor, this.bodyColor, this.accentColor,
      this.textColor, this.subTextColor);
}
